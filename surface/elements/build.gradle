defaultTasks "buildAll"


task buildHtml (type:Exec) {
	executable "/bin/sh"
	args "-c", "make-all.sh"
}

task stage (type:Task) {
	dependsOn buildHtml
	doLast(){
		ant.gzip( src:"elements.html",destfile:"elements.html.gz")
		ant.copy( file:"elements.html.gz", todir:"${surfaceBuild}",overwrite:"true")

	}
}

task buildAll (type:Task) {
	dependsOn stage
}
